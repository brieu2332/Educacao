let slotColor = "rgb(246, 227, 148)";
const slots = [];
const slotWidth = 100;
const slotHeight = 150;

// Criar slot
function createSlot(x, y) {
    return {
        x: x,
        y: y,
        width: slotWidth,
        height: slotHeight,
    };
}

// Desenhar slot
function drawSlot(slot) {
    
}
